// The release workflow (.github/workflows/release.yml) rewrites this line with the tag before
// it builds the binary it signs and notarizes; a source build reports "dev".
let systemoneVersion = "dev"
